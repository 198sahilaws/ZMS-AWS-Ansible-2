# Pinned to ranges that resolve to PURE-PYTHON wheels on every distro in the
# estate (Ubuntu 24.04 / Amazon Linux 2023 / RHEL 9 / SLES 15). Nothing here
# compiles, so no host needs gcc, python3-devel or mariadb-devel — which is the
# single biggest cause of "works on Ubuntu, fails on SLES" in a mixed lab.
#
# PyMySQL is used instead of mysqlclient for exactly that reason: mysqlclient is
# a C extension against libmariadb, PyMySQL is a pure-Python implementation of
# the MySQL wire protocol.
Flask>=2.2,<4
Jinja2>=3.0,<4
PyMySQL>=1.0,<2
requests>=2.28,<3
gunicorn>=20.1,<24
Faker>=18.0,<40
