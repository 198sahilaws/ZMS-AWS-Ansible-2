"""ZMS lab service desk — a small Flask + MySQL application.

Exists to put realistic, continuously changing data in front of the Ubuntu
MySQL host and to make the web host talk to it over the network, so the estate
produces enterprise-shaped east-west traffic instead of an idle port.
"""
__version__ = "1.0.0"
